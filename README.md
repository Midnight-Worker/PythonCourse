# PythonCourse

Installation:

powershell als admin öffnen

wsl --install

wsl -- install -d Ubuntu

Name und passwort vergeben

wget -qO- https://raw.githubusercontent.com/Midnight-Worker/PythonCourse/main/wsl_ubuntu_setup.sh | bash

git config --global.username "dein name"
git config --global.email "deine@mail.de"

git clone https://github.com/Midnight-Worker/PythonCourse.git

git tag

git checkout losGehts

